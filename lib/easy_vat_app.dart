import 'package:flutter/material.dart';

class EasyVatApp extends StatelessWidget {
  const EasyVatApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
    );
  }
}
